$output = '';

if (isset($_POST['searchloc'])) {
    $searchq = mysqli_real_escape_string($conn, $_POST['searchloc']);

    $query = "SELECT * FROM locations 
    WHERE l_name LIKE '%$searchq%'
    OR l_area LIKE '%$searchq%'
    OR l_zip LIKE '%$searchq%'";
    $count = mysqli_num_rows($query);
    if ($count == 0) {
        $output = 'There was no search results!';
    } else {
        while ($rows = $res->fetch_assoc()) {
            $lname = $rows['l_name'];
            $larea = $rows['l_area'];
            $lzip = $rows['l_zip'];
            $lphone = $rows['l_phone'];
            $ldesc = $rows['l_desc'];
            $lgmap = $rows['l_gmap'];
        }
    }
}